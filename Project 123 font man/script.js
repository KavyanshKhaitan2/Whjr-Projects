function preload() {

}

function setup() {
  canvas = createCanvas(500, 500)
  canvas.center()
  canvas.position(window.innerWidth - 600, "")

  video = createCapture(VIDEO)
  video.size(500, 500)
  video.center()
  video.position(100, "")
}

function draw() {

}